#include <iostream>
#include "driver.h"
#include "address.h"
#include "date.h"
using namespace std;

int main() {
	int numInput;
	string wordInput;
	bool running = true;

	do {
		//menu loop
	} while (running);
	
	return 0;
}